---
const {id} = Astro.props;
---
<section
  id={id}
  class="w-full h-full flex justify-center py-16 sm:px-14 xl:px-56 2xl:px-96"
>
  <article
  class="w-full h-full flex flex-col gap-4 justify-between items-center max-w-[1280px]"
  >
  <slot/>           
  </article>    
</section>